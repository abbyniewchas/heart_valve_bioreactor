% This script serves as a low-level editor for lab data to be changed and
% used by the bioreactor gui

% Ted Lutkus
% 1/13/2020

% Users
lab_data.users = {'Ted, Teb'};

% Experiment names
lab_data.experiments.names = {'Single Valve', 'Multi Valve'};

% Experiment parameters must be changed through gui
lab_data.experiments.default_parameters = 


lab_data.save_location = {'\gui_data